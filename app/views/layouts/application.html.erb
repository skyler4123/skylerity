<!DOCTYPE html>
<html>
  <head>
    <title>Skylerity</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render(Nav::User::SessionComponent.new(current_user: @current_user)) %>
    <%= yield %>
  </body>
</html>
